---
title: 'JavaScript เบื้องต้น'
description: 'ภาพรวมของ JavaScript ในฐานะภาษาที่ขับเคลื่อน Node.js และสิ่งที่โมดูลนี้ครอบคลุม'
sidebar:
  order: 1
---

import NodeRunner from '../../../../components/NodeRunner.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## JavaScript คือภาษาของ Node.js

Node.js คือ runtime ที่รัน JavaScript นอก browser ทุกอย่างที่เขียนใน Node ตั้งแต่ HTTP server ง่ายๆ ไปจนถึง CLI tool ที่ซับซ้อน ล้วนเป็น JavaScript ดังนั้นการเข้าใจภาษาอย่างแน่นหนาจึงเป็นสิ่งจำเป็นก่อนที่จะลงลึกใน API ของ Node

โมดูลนี้ครอบคลุม JavaScript พื้นฐานที่นักพัฒนามักสะดุดมากที่สุดในระบบนิเวศ Node:

| บทเรียน | หัวข้อ |
|---------|--------|
| Types และ Coercion | Primitives vs objects, `typeof`, `==` vs `===`, truthy/falsy |
| Scope และ Closures | `var`/`let`/`const`, block scope, hoisting, lexical scope |
| `this` และ Context | Dynamic `this`, `call`/`apply`/`bind`, arrow functions |
| Modules | ESM `import`/`export` vs CommonJS `require`, ใช้เมื่อไหร่ |

JavaScript สมัยใหม่หมายถึง **ES2022 ขึ้นไป** ตลอดทั้งโมดูล — `const`/`let`, arrow functions, destructuring, `async`/`await`, optional chaining และ native ES modules โมดูลนี้ยึด Node 24 (LTS ตัวปัจจุบัน) และทุกอย่างรันบน Node 26 ได้เช่นกัน

## Demo แรกที่รันได้

โค้ดด้านล่างแสดงแนวคิด JS หลายอย่างพร้อมกัน: การประกาศ `const`, ฟังก์ชัน, การต่อสตริงแบบไม่ใช้ template literal และ closure เหนือตัวแปรภายนอก ลองรันและสังเกตผลลัพธ์

```js
const greet = function(name) {
  return 'Hello, ' + name + '!';
};

const languages = ['JavaScript', 'TypeScript', 'Node.js'];

for (const lang of languages) {
  console.log(greet(lang));
}
```

export const indexDemoCode = `
const greet = function(name) {
  return 'Hello, ' + name + '!';
};

const languages = ['JavaScript', 'TypeScript', 'Node.js'];

for (const lang of languages) {
  console.log(greet(lang));
}
`;

<NodeRunner client:visible code={indexDemoCode} />

## ฟีเจอร์ภาษาใหม่ ๆ ที่คุณจะได้เจอ

Node อัปเกรด V8 ทุก major release ภาษาจึงโตขึ้นเรื่อย ๆ:

- **Node 24** — `using` / `await using` สำหรับเคลียร์ resource อัตโนมัติ, `RegExp.escape()`, `Error.isError()`, `Float16Array`
- **Node 26** — API วันเวลา **Temporal** เปิดเป็นค่าเริ่มต้น พร้อม `Map.prototype.getOrInsert()` และ `Iterator.concat()`

```js
// Node 26: Temporal แทนการคำนวณ Date ที่ผิดพลาดง่าย
const due = Temporal.Now.plainDateISO().add({ days: 30 });
console.log(due.toString()); // เช่น 2026-10-12

// Node 24: using — resource ถูก dispose เมื่อออกจาก block
{
  using conn = openConnection();   // object นี้มีเมธอด [Symbol.dispose]()
  conn.query('SELECT 1');
}                                   // conn[Symbol.dispose]() ทำงานตรงนี้ แม้จะ throw
```

<Callout title="Modern JS throughout">
โมดูลนี้ใช้ syntax ES2022+ หากพบฟีเจอร์ที่ไม่คุ้นเคย MDN Web Docs คือแหล่งอ้างอิงที่ดีที่สุด — และรายการด้านบนบอกว่าฟีเจอร์นั้นมาพร้อม Node สายไหน
</Callout>

export const indexQuiz = [
  {
    q: 'Node.js ให้สิ่งที่ browser ไม่มีคืออะไร?',
    options: [
      'JavaScript engine',
      'การเข้าถึง file system, network sockets และ OS APIs โดยไม่มี browser sandbox',
      'รองรับ `const` และ `let`',
      'Garbage collection',
    ],
    answer: 1,
  },
  {
    q: 'โมดูลนี้พึ่งพาฟีเจอร์ JavaScript เวอร์ชันใด?',
    options: [
      'ES5 เท่านั้น — เพื่อความเข้ากันได้สูงสุด',
      'ES2015 (ES6) เท่านั้น',
      'ES2022 ขึ้นไป บน Node 24+',
      'TypeScript ที่คอมไพล์ลงเป็น ES3',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="js-essentials/index" questions={indexQuiz} />

<ProgressTracker client:visible id="js-essentials/index" />
